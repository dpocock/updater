diff --git a/builder/builder.pro b/builder/builder.pro
index fa94a46..3627438 100644
--- a/builder/builder.pro
+++ b/builder/builder.pro
@@ -15,15 +15,15 @@ CONFIG += qt warn_on thread
 
 DESTDIR = ../bin
 
-LIBS += -L../lib -lupdatercommon -L../$${OPENRPT_BLD}/lib -lcommon -lrenderer
+LIBS += -L../lib -lupdatercommon -L$${OPENRPT_BLD} -lMetaSQL -lrenderer -lopenrptcommon
 win32-msvc* {
   PRE_TARGETDEPS += ../lib/updatercommon.lib          \
                     ../$${OPENRPT_BLD}/lib/common.lib \
                     ../$${OPENRPT_BLD}/lib/renderer.lib
 } else {
   PRE_TARGETDEPS += ../$${UPDATER_BLD}/lib/libupdatercommon.a \
-                    ../$${OPENRPT_BLD}/lib/libcommon.a \
-                    ../$${OPENRPT_BLD}/lib/librenderer.a
+                    $${OPENRPT_BLD}/libopenrptcommon.so \
+                    $${OPENRPT_BLD}/librenderer.so
 }
 
 MOC_DIR = tmp
diff --git a/global.pri b/global.pri
index 62ab223..00d5589 100644
--- a/global.pri
+++ b/global.pri
@@ -8,48 +8,14 @@
 # to be bound by its terms.
 #
 
-exists(../../../openrpt) {
-  OPENRPT_DIR = ../../../openrpt
-}
-exists(../../openrpt) {
-  OPENRPT_DIR = ../../openrpt
-}
-exists(../openrpt) {
-  OPENRPT_DIR = ../openrpt
-}
-
-! exists($${OPENRPT_DIR}) {
-  error("Could not set the OPENRPT_DIR qmake variable.")
-}
-
-OPENRPT_BLD=$${OPENRPT_DIR}
-exists($${OPENRPT_DIR}-build-desktop) {
-  OPENRPT_BLD=$${OPENRPT_DIR}-build-desktop
-}
+OPENRPT_DIR = /usr/include/openrpt
+OPENRPT_BLD = /usr/lib/$$(DEB_HOST_MULTIARCH)
 
-exists(../../../xtuple) {
-  XTUPLE_DIR = ../../xtuple
-}
-exists(../../../xtuple/trunk) {
-  XTUPLE_DIR = ../../xtuple/trunk
-}
-exists(../../xtuple) {
-  XTUPLE_DIR = ../../xtuple
-}
-exists(../../xtuple/trunk) {
-  XTUPLE_DIR = ../../xtuple/trunk
-}
-exists(../xtuple) {
-  XTUPLE_DIR = ../xtuple
-}
-exists(../xtuple/trunk) {
-  XTUPLE_DIR = ../xtuple/trunk
-}
+CSVIMP_DIR = /usr/include/csvimp
+CSVIMP_BLD = /usr/lib/$$(DEB_HOST_MULTIARCH)
 
-XTUPLE_BLD=$${XTUPLE_DIR}
-exists($${XTUPLE_DIR}-build-desktop) {
-  XTUPLE_BLD=$${XTUPLE_DIR}-build-desktop
-}
+XTUPLE_DIR = /usr/include/xtuple
+XTUPLE_BLD = /usr/lib/xtuple
 
 UPDATER_BLD=.
 exists(../updater-desktop-build) {
@@ -60,10 +26,10 @@ message("Looking for OpenRPT code in $${OPENRPT_DIR}.")
 message("Looking for xTuple code in $${XTUPLE_DIR}.")
 
 INCLUDEPATH += ../common \
-               ../$${OPENRPT_DIR}/common \
-               ../$${OPENRPT_DIR}/MetaSQL \
-               ../$${XTUPLE_DIR}/common  ../$${XTUPLE_BLD}/common
-DEPENDPATH  += ../$${UPDATER_BLD}/common ../$${OPENRPT_BLD}/common ../$${OPENRPT_BLD}/MetaSQL
+               $${OPENRPT_DIR}/common \
+               $${OPENRPT_DIR}/MetaSQL \
+               $${XTUPLE_DIR}/common
+DEPENDPATH  += ../$${UPDATER_BLD}/common $${OPENRPT_BLD} $${XTUPLE_BLD}
 
 INCLUDEPATH = $$unique(INCLUDEPATH)
 
diff --git a/loader/loader.pro b/loader/loader.pro
index 2792498..aaaf776 100644
--- a/loader/loader.pro
+++ b/loader/loader.pro
@@ -12,14 +12,14 @@ include( ../global.pri )
 
 TEMPLATE = app
 CONFIG += qt warn_on
-DEPENDPATH  += ../$${XTUPLE_BLD}/common
+DEPENDPATH  += $${XTUPLE_BLD}
 
 TARGET = updater
 OBJECTS_DIR = tmp
 MOC_DIR     = tmp
 UI_DIR      = tmp
 
-LIBS += -L../$${OPENRPT_BLD}/lib -L../$${XTUPLE_BLD}/lib -lxtuplecommon -L../lib -lupdatercommon -lcommon -lrenderer
+LIBS += -L$${OPENRPT_BLD} -L$${XTUPLE_BLD} -lxtuplecommon -L../lib -lupdatercommon -lMetaSQL -lrenderer -lopenrptcommon
 macx: LIBS += -lz
 
 win32-msvc* {
@@ -28,8 +28,8 @@ win32-msvc* {
                     ../$${OPENRPT_BLD}/lib/renderer.lib
 } else {
   PRE_TARGETDEPS += ../lib/libupdatercommon.a          \
-                    ../$${OPENRPT_BLD}/lib/libcommon.a \
-                    ../$${OPENRPT_BLD}/lib/librenderer.a
+                    $${OPENRPT_BLD}/libopenrptcommon.so \
+                    $${OPENRPT_BLD}/librenderer.so
 }
 
 DESTDIR = ../bin
diff --git a/updater.pro b/updater.pro
index a62e790..6837fff 100644
--- a/updater.pro
+++ b/updater.pro
@@ -8,45 +8,14 @@
 # to be bound by its terms.
 #
 
-exists(../../openrpt) {
-  OPENRPT_DIR = ../../openrpt
-}
+OPENRPT_DIR = /usr/include/openrpt
+OPENRPT_BLD = /usr/lib/$$(DEB_HOST_MULTIARCH)
 
-exists(../openrpt) {
-  OPENRPT_DIR = ../openrpt
-}
+CSVIMP_DIR = /usr/include/csvimp
+CSVIMP_BLD = /usr/lib/$$(DEB_HOST_MULTIARCH)
 
-! exists($${OPENRPT_DIR}) {
-  error("Could not set the OPENRPT_DIR qmake variable.")
-}
-
-
-OPENRPT_BLD=$${OPENRPT_DIR}
-exists($${OPENRPT_DIR}-desktop-build) {
-  OPENRPT_BLD=$${OPENRPT_DIR}-desktop-build
-}
-
-exists(../xtuple) {
-  XTUPLE_DIR = ../xtuple
-}
-exists(../xtuple/trunk) {
-  XTUPLE_DIR = ../xtuple/trunk
-}
-exists(../../xtuple) {
-  XTUPLE_DIR = ../../xtuple
-}
-exists(../../xtuple/trunk) {
-  XTUPLE_DIR = ../../xtuple/trunk
-}
-
-! exists($${XTUPLE_DIR}) {
-  error("Could not set the XTUPLE_DIR qmake variable.")
-}
-
-XTUPLE_BLD=$${XTUPLE_DIR}
-exists($${XTUPLE_DIR}-desktop-build) {
-  XTUPLE_BLD=$${XTUPLE_DIR}-desktop-build
-}
+XTUPLE_DIR = /usr/include/xtuple
+XTUPLE_BLD = /usr/lib/xtuple
 
 TEMPLATE = subdirs
 SUBDIRS = common \
